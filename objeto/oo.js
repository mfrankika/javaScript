// CÓDIGO NÃO EXECUTÁVEL!!!

// Procedural
//função que recebe/manipula dados 
processamento(valor1, valor2, valor3)


 // OO
 // dados(objetos) que tem funções e compportamentos(metodos)
 objeto = {
     valor1, // = atributos
     valor2,
     valor3,
     processamento() {  /* que é comportamentos = funções */
         // ...
     }
 }

 // invocar apartir dos dados(objetos)
 objeto.processamento() // Foco passou a ser o objeto
                        // a função pertence ao objeto
 // Principios importantes:
 // 1. abstracao
 // 2. encapsulamento
 // 3. herança (prototype)
 // 4. polimorfismo //multipla forma - reeditar a herança 


 // composição = tem 1
 // herança = é um